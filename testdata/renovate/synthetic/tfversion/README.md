# Synthetic corpus tree

Synthetic corpus input: no estate repository exercises `terraform-version` or
`kustomize` yet, so this tree does, with the shapes the managers document.
Captured like every other corpus entry by executing the pinned container over
it (`tools/capture/extract-corpus.sh testdata/renovate/synthetic/tfversion`
after `git init` of a staging copy). Synthetic input, recorded behaviour.
